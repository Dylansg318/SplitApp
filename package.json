{
  "name": "receipt-splitter",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "Split a restaurant bill from a photo. On-device OCR, no LLM, no network, no cost.",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "fixtures": "tsx scripts/make-fixtures.ts",
    "bakeoff": "tsx scripts/bakeoff.ts",
    "bench:preprocess": "tsx scripts/preprocess-bench.ts"
  },
  "dependencies": {
    "preact": "^10.29.8",
    "tesseract.js": "^7.0.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "@types/node": "^26.4.0",
    "sharp": "^0.35.4",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vite": "^7.3.6",
    "vitest": "^3.2.7"
  }
}
